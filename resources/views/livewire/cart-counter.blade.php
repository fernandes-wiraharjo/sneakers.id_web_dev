<div>
    {{-- Close your eyes. Count to one. That is how long forever feels. --}}
    <span id="cartCounter" class="Header__CartDot {{$cartCounter > 0 ? 'is-visible' : ''}}">{{ $cartCounter ?? 0 }}</span>
</div>
